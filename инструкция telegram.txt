1. Установить git
	sudo apt-get update
	sudo apt-get install git

2. Установить telegram api, как в https://github.com/vysheng/tg
	git clone --recursive https://github.com/vysheng/tg.git && cd tg
	sudo apt-get install libreadline-dev libconfig-dev libssl-dev lua5.2 liblua5.2-dev libevent-dev libjansson-dev libpython-dev make 
	./configure
	 make

3. Установить nodejs
	sudo apt-get update
	sudo apt-get install nodejs
	sudo ln -s /usr/bin/nodejs /usr/bin/node
	npm install socket.io 						//из папки с сервером
4. 

Полезные команды:
	bin/telegram-cli -k tg-server.pub			//запуск telegram клиента

	ifconfig - узнать ip

	Change to en_US.utf8:
	$ export LC_ALL=en_US.utf8 
	$ export LANG="$LC_ALL"
	http://help.ubuntu.ru/wiki/russian_font_in_console


http://linsovet.org.ua/elinks-hot-keys

https://github.com/KazarinAY/telenode.git

centOS:
1. Установить git
	sudo yum install git

2. Установить telegram api, как в https://github.com/vysheng/tg
	git clone --recursive https://github.com/vysheng/tg.git && cd tg
	sudo yum install lua-devel openssl-devel libconfig-devel readline-devel libevent-devel libjansson-devel python-devel
	sudo apt-get install libreadline-dev libconfig-dev libssl-dev lua5.2 liblua5.2-dev libevent-dev libjansson-dev libpython-dev make 
	./configure
	 make

3. Установить nodejs
	https://github.com/joyent/node.wiki.git

		Run as root on RHEL, CentOS or Fedora:

		curl -sL https://rpm.nodesource.com/setup | bash -
		Then install, as root:

		yum install -y nodejs
		Optional: install build tools

		To compile and install native addons from npm you may also need to install build tools:

		yum install gcc-c++ make
		# or: yum groupinstall 'Development Tools'